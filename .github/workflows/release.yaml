name: release

on:
    push:
        branches: ["main"]
    pull_request:
      types:
        - closed

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
    release:
        name: Release
        timeout-minutes: 15
        runs-on: ubuntu-latest
        permissions:
            contents: read
            pull-requests: write
        if: github.event.pull_request.merged == true && github.event.pull_request.title == 'Release'
        steps:
            - name: Check out code
              uses: actions/checkout@v3
              with:
                  fetch-depth: 2
                  token: ${{ secrets.SEMANTIC_RELEASE_TOKEN }}
            - name: Setup Node.js environment
              uses: actions/setup-node@v3
              with:
                  version: latest
            - name: Install dependencies
              run: npm install
            - name: Update package json with new version
              run: |
                npx changeset version
                npx changeset tag
                npm run translation
              env:
                GITHUB_TOKEN: ${{ secrets.SEMANTIC_RELEASE_TOKEN }}
